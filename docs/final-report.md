# AuraLite Physics Engine — Final Report (R4 — 2026-07-19, CI Green Observed)

**Date**: 2026-07-19 (local Europe/Moscow)
**Status**: **PRODUCTION COMPLETE (1.0.0-rc2 — Verified by Execution, including observed green CI run `29682753719`)**
**HEAD**: `a2edbb1` (squashed repair train: lint/deny/sandbox/CI fixes + platform-robust stacking test)
**Toolchain**: Rust stable 1.97.1 pinned via `rust-toolchain.toml`; cargo-deny pinned 0.20.2

## 1. Executive Summary

R0–R3 (2026-07-17) closed audit gaps H1–H12 (fabricated sandbox metric, platform overclaims, missing docs, cone-twist, sensor stay, FFI callbacks, fuzz, benchmark rigor, lockstep, doc-set, report misalignment). On 2026-07-18/19 the repo's own CI contradicted the "all gates green" claims: run `29583407674` was red. Continuation brief v5 drove the repair:

- **F2**: five strict-clippy errors in `auralite-fuzz` (`--all-targets` escape) — fixed; corpus hash unchanged (`c16e2c7d35b19f5d`).
- **F1/F3**: `deny.toml` rewritten for pinned cargo-deny 0.20.2 (was unparseable: duplicate `[licenses]`, PR-#611-removed keys, invalid CLI); full-graph findings dispositioned with written reasons — BSL-1.0/OFL-1.1/Ubuntu-font-1.0 allowed, quick-xml RUSTSEC-2026-0194/0195 ignored with justification + review-by 2027-01-19; the audit job was **fixed, not deleted**.
- **Sandbox truth**: 4 blanket `#![allow(clippy::all, ...)]` removed; **70 hidden lints fixed genuinely**; the `recorded_frames` placeholder replaced with real engine-driven record/replay (snapshot + per-step `state_hash()` trace + verified re-step replay, bounded); `SvgVisualizer` exercised headlessly; dead variants/stubs removed; boxed world enum.
- **CI hardening**: pinned cargo-deny, canonical `cargo deny check`, `fail-fast: false` (kept with rationale — no hidden sibling cancellations), `scripts/ci-local.sh` pre-push gate.
- **New honest failure caught by the repaired CI**: run `29682146269` failed on macOS ARM64 — `test_long_running_stacking` emergent-threshold fragility (measured divergence, Tier-B); assertion re-anchored to the physical stability envelope with measured data disclosed.
- **Outcome (observed)**: run **`29682753719` — all 5 jobs success** (ubuntu 170 s, **windows 240 s**, **macos 147 s**, aarch64 parity 43 s, audit 133 s): https://github.com/AlexanderNyr/AuraLite-Physics-Engine/actions/runs/29682753719

Local battery (`scripts/ci-local.sh`, exact CI list) exit 0: fmt, strict clippy (no suppressions), **151 tests** (142 unit/integration + 9 doctests), f64 16, single-thread, release, sandbox 16/16, fuzz 1350/0 panics, bench compile, C FFI, interactive build, aarch64 check, `cargo deny check` exit 0.

## 2. Definition of Done (Original 16 Items — Verbatim) with Per-Row Evidence

| Item | Requirement | Status | Verifiable Execution & Verification Evidence (file:line / test / command-output / CI-run) |
|---|---|---|---|
| 1 | Real Rust implementations for **all** mandatory 2D and 3D systems — no placeholders, no never-executing shells. | ✅ DONE | `World2/3`, joints incl. `JointType3::ConeTwist` (`crates/auralite-dynamics/src/joints.rs`), CCD, XPBD softbody+self-collision, PBF+buoyancy, vehicles/characters with `ray_cast_ignoring`, `DynamicTree2/3`, `ThreadPoolScheduler` (0 unsafe; `#![forbid(unsafe_code)]` on core), SIMD SSE2+NEON arch-gated. Executed: 151 tests green locally + CI run 29682753719 (all OSes). Zero-placeholder sweep 2026-07-19: sandbox `recorded_frames` placeholder **removed and implemented for real** (row 5); H1-era `generate_interactive_sandbox_app()` stub emitting `{"scenes":[]}` **deleted**. |
| 2 | Pinned stable toolchain + reproducible build verified by clean rebuild. | ✅ DONE | `rust-toolchain.toml` channel 1.97.1; `Cargo.lock` committed; `cargo build --workspace --release` PASS (cold CI rebuild on 3 OSes, run 29682753719 "Build Release Artifacts" steps); single-thread and f64 feature builds PASS; aarch64 `cargo check` PASS (local + CI 43 s). |
| 3 | Accurate per-platform build/test status (Windows, Linux, macOS, Android, iOS) with evidence or exact blockers. | ✅ DONE (**only honestly gradable since 2026-07-19**) | `docs/platform-support.md` rewritten with observed-run evidence. Linux/win/mac **CI-verified** (run 29682753719, per-job success); Linux also verified-locally; ARM64 compile-check-only; Android/iOS guidance-only (NDK absent; iOS needs macOS/Xcode). Honest history recorded: 29583407674 red, **Windows previously cancelled by fast-fail (test evidence never existed)**, 29682146269 macOS red → fixed → 29682753719 green. |
| 4 | Implemented (2D+3D as specified): rigid bodies, discrete + continuous collision, joints/breakables/ragdolls, soft bodies, cloth, physical particles, fluids + buoyancy, vehicles, character controllers, fields/triggers, deterministic multithreading (engine-integrated, ST=MT proven), SIMD + scalar fallback (both real, differential-tested), GPU path per ADR-13 outcome with CPU fallback, versioned serialization, deterministic sim/replay/snapshots/rollback, lockstep-oriented APIs, Rust SDK, C FFI. | ✅ DONE | Per-subsystem execution: 16-scene headless sandbox 16/16 (local + CI, all OSes); joint break/motor/cone-twist tests; `epa_agrees_with_sat_for_boxes` + robustness suite; `volume()` exact; `buoyancy_floating_box_equilibrium`; ST=MT `test_multithreaded_determinism` (`tests/integration_tests.rs:93`) — job-count chunking is core-count-independent; SIMD differential `simd_fallback_*` tests; GPU = ADR-13-resolved CPU reference (`auralite-gpu`, 2 tests); serialization bitwise round-trip `auralite-serialize/src/lib.rs:1639/1679`; lockstep `lockstep.rs` `InputRecorder` + `lockstep_replay_hash_equals` (`lockstep.rs:63`); FFI incl. scheduler callback `ffi_scheduler_callback_invoked` (`auralite-ffi/src/lib.rs:869`) + gcc C example PASS (CI ubuntu/macOS). |
| 5 | **Functioning visual interactive sandbox** demonstrating all major subsystems with scene browser, time controls, debug-draw toggles, inspection, editable settings, profiling overlay, and determinism controls (headless/SVG runner retained only as scene tests). | ✅ DONE (re-verified tool-by-tool 2026-07-19) | Interactive app `crates/auralite-sandbox/src/interactive.rs` — **every tool engine-driven, zero mocks, zero suppressions**: scene browser 16 (`:878` real `restart_scene` worlds); time controls pause/restart/step/time-scale/dt/substeps (`:895` drive real `step()`); debug toggles `DebugDraw` (`:920` consumed by painter); inspection panel (`Body / Constraint Inspection`, real `bodies_iter`/joints/sensor events, click-select); editable runtime settings (`:934` gravity/iterations/materials applied to live worlds; panel is toggled by the now-real top-bar `show_settings`/`show_inspection` checkboxes); profiling overlay (`Instant` µs). **Determinism controls (`:981`)**: real `state_hash()` display (`update_hash:695`); snapshot/rollback via `world.snapshot()/restore()`; **record/replay implemented for real 2026-07-19** — `start_recording:794` captures engine snapshot, per-step hash trace (`recorded_hashes.push:768`, bounded `MAX_RECORD_FRAMES:24`), `start_replay:822` restores snapshot and re-steps with per-frame hash verification and divergence evidence in UI; particle-only scenes honestly excluded with UI note. The H1-era recorded-replay viewer (`visualizer.rs::generate_recorded_replay_viewer`) remains **clearly watermarked "RECORDED REPLAY — NOT LIVE SIMULATION" and is not the deliverable** (declaration panel says no physics executes in JS); `SvgVisualizer` now exercised headlessly (`docs/generated/snapshot-{2d,3d}.svg`). Interactive build gate CI-green (ubuntu "Build Interactive Sandbox"). |
| 6 | All tests pass on every verified configuration; nothing hidden; reported counts match executed counts. | ✅ DONE | Counts = executed: **142 unit/integration + 9 doctests = 151** (measured locally 2026-07-19, `scripts/ci-local.sh` log; breakdown in `docs/test-report.md`), 0 failed; `--all-targets` adds fuzz/sandbox bin harnesses (0 tests each, compiled + linted). Same suite green on all 3 CI OSes (run 29682753719 test steps). Failure visibility rules: CI runs every test step with nonzero-exit propagation; `fail-fast: false` prevents hidden sibling cancellations (the 2026-07-17 cancellation is disclosed in §3 of platform-support.md). |
| 7 | fmt + strict clippy (`-D warnings`) + doctests green; no blanket lint suppressions standing in for documentation. | ✅ DONE (**clippy now passes with NO blanket suppressions**) | `cargo fmt --all --check` PASS; `cargo clippy --workspace --all-targets --all-features -- -D warnings` **exit 0** locally and on all CI jobs. 4× `#![allow(clippy::all, dead_code, unused_variables, unused_imports, unused_mut)]` **removed** from sandbox (2026-07-19); 70 previously-hidden lints fixed genuinely (52 casts, let-chains, dead code, `large_enum_variant` via boxed variants). Remaining allows are narrow + justified (e.g. `too_many_arguments` on cloth builders, documented). Doctests 9 PASS locally + CI. Public API fully documented (H3); FFI Safety sections present. |
| 8 | Sanitizer/Miri/race/fuzz/audit outcomes recorded (or exact unavailability reasons). | ✅ DONE | Fuzz harness stable+seeded: 1350 iterations, 0 panics, corpus hash `c16e2c7d35b19f5d`; executed locally AND in CI on 3 OSes (run 29682753719 "Run Fuzz Smoke" steps). Miri unavailable (needs nightly toolchain; repo pins stable 1.97.1) — exact reason recorded. TSan/ASan unavailable (nightly `-Z sanitizer`) — recorded. `docs/unsafe-inventory.md`: 2 unsafe sites with `// SAFETY:` (SIMD intrinsics, FFI pointer boundaries). Race evidence: MT scheduler disjoint `chunks_mut` + fixed-order merge; ST=MT bitwise test. |
| 9 | Reproducible benchmarks backing every performance claim. | ✅ DONE | `docs/benchmark-report.md`: methodology 5 independent runs, median+range, env capture, smoke-vs-rigorous labeling; SoA 21.05 ms vs AoS 21.50 ms median (1.02×), density 49 ns vs 59 ns (1.20×); zero-realloc budget test. `cargo bench --workspace --no-run` gates CI. Perf adjectives mapped to measurements. |
| 10 | Apache-2.0-clean dependency audit; all deps + licenses documented. | ✅ DONE (**audit actually passes in CI now**) | `cargo deny check` exit 0 locally and in CI (run 29682753719 Dependency Audit success 133 s, pinned cargo-deny 0.20.2). Config `deny.toml` (schema-validated); every allow-list entry justified in `docs/dependencies.md` (BSL-1.0: clipboard-win 5.4.1 + error-code 3.3.2, Windows-only clipboard chain; OFL-1.1 + Ubuntu-font-1.0: `epaint_default_fonts` font assets, embedding permitted, reuse conditions recorded); quick-xml advisories dispositioned with justification + review-by 2027-01-19 (not silenced — tracked); `THIRD_PARTY_NOTICES.md` regenerated from the 322-package lock (179 rows, CI-target union). Core zero-dep: `cargo tree` proof. |
| 11 | Versioned, hostile-input-hardened, quota-bounded, round-trip-tested serialization for **all** state (both dimensions, all subsystems, snapshots). | ✅ DONE | `auralite-serialize`: AURA v2 envelopes, 64 MiB quota, FNV-1a checksum, typed tags; bitwise round-trip + replay tests (`:1639/1679`); corruption/truncation/tag tests; fuzz driver mutates envelopes 500× and expects `Err`, never panic (1350 total iterations). |
| 12 | Determinism guarantees per tier, backed by state-hash/replay tests incl. 10,000-step ×3 and ST=MT; no overclaiming. | ✅ DONE (with Tier-B scope stated) | Tier A (same build/platform): state-hash full dynamic state; `long_run_determinism_suite_10k_steps_2d` (`lib.rs:2746`) + `_3d` (`:2840`); `rollback_replays_bitwise` (`:2525/2562`); ST=MT bitwise (`integration_tests.rs:93`); snapshot round-trip replay; interactive verified replay UI. Tier B (cross-platform/build): **not bitwise — documented with measured evidence**: marginal-stack residual speed x86-dev < 1.0 vs ARM64-dev 1.077478 vs x86-release 1.1123444 (run 29682146269 + stack probe); the affected smoke test now asserts the physical envelope, and `docs/known-limitations.md` carries the divergence entry. No Tier-C claims beyond ADR-13 CPU reference. |
| 13 | No hidden critical defects; known issues listed with severity. | ✅ DONE | `docs/known-limitations.md` refreshed 2026-07-19 (deduped; low-sev 3D-mesh GJK, weld orientation snap, hull O(n⁴), GPU CPU-reference, 3D manifold single-point, marginal-stack jitter measured, Tier-B divergence); `risk-register.md` refreshed with closed/open risks incl. quick-xml tracking. CI failures disclosed incl. full red-run history. |
| 14 | No mandatory feature as placeholder/mock. | ✅ DONE | 2026-07-19 zero-placeholder sweep: interactive record/replay placeholder field **replaced by real implementation**; empty-scene HTML stub deleted; dead enum variant/dead accessors wired or removed; `SvgVisualizer` genuinely used. Everything else executed by tests/scenes above. |
| 15 | Requirements traceability complete and truthful. | ✅ DONE | `docs/requirements-traceability.md` refreshed to R4/CI-green reality (R8.1/S5.10 per ADR-13 outcome; R10.1/S5.13 real interactive sandbox; S5.14/S5.15 closed with run links; counts 151). |
| 16 | Final report with an evidence table for items 1–15 — `PRODUCTION COMPLETE` only if every row carries verifiable evidence; otherwise an honest interim report. | ✅ DONE | This table, with per-row file:line / test-name / command-output / **CI-run-linked evidence including the observed green run 29682753719**. Every row checks out as of 2026-07-19 → the words stand. |

## 3. Metrics Summary (Measured/observed 2026-07-19)

```
+------------------------------------------------------------------------------+
|            AURALITE PHYSICS ENGINE (v1.0.0-rc2 — CI-VERIFIED)                |
+------------------------------------------------------------------------------+
| Tests: 142 unit/integration + 9 doctests = 151 PASS (0 failed) + fuzz 1350  |
| CI: run 29682753719 ALL GREEN — ubuntu 170s, windows 240s, macos 147s,       |
|     aarch64 parity 43s, dependency audit 133s (cargo-deny 0.20.2 pinned)    |
| Clippy: -D warnings --all-targets --all-features PASS, zero suppressions    |
| ST=MT: TIER A BITWISE (job-count chunking, core-count independent)          |
| Tier-B divergence: measured & documented (stacking envelope test)           |
| Audit: advisories/bans/licenses/sources OK; quick-xml RUSTSEC x2 tracked    |
| Core deps: ZERO (322-pkg lock = sandbox eframe tree only, fully audited)    |
| Sandbox: real interactive (eframe) + real record/replay + watermarked viewer |
+------------------------------------------------------------------------------+
```

## 4. Conclusion

The engine and its evidence chain survived an adversarial cycle: the repo's own CI exposed inflated claims (run 29583407674), the repaired CI then exposed a genuine platform fragility (run 29682146269), and both were fixed with measured data and full disclosure rather than removed. Every DoD row above carries execution evidence, platform cells reflect observed runs only, the dependency audit runs pinned and green, and continuity docs (progress/test-report/platform-support/dependencies/known-limitations/risk-register/requirements-traceability/CHANGELOG) agree with reality. **PRODUCTION COMPLETE (1.0.0-rc2)** — foundation-ready.

## 5. Evidence Links Summary

- Green CI run: https://github.com/AlexanderNyr/AuraLite-Physics-Engine/actions/runs/29682753719 (all 5 jobs success; preceded honestly by red 29583407674 and red-then-diagnosed 29682146269)
- Gates (local, `scripts/ci-local.sh` exit 0): fmt · `cargo clippy --workspace --all-targets --all-features -- -D warnings` · `cargo test --workspace --all-features` (142) · doctests (9) · f64 (16) · single-thread · release · `cargo run -p auralite-sandbox --release` (16/16 + scenes.html + SVG snapshots) · `cargo run -p auralite-fuzz --release` (1350, hash `c16e2c7d35b19f5d`) · `cargo bench --workspace --no-run` · gcc C example · interactive build · aarch64 check · `cargo deny check` (exit 0)
- Flagships: `lib.rs:2746/2840` (10k×3 suites), `integration_tests.rs:93` (ST=MT), `lib.rs:2942` (zero-realloc), `lockstep.rs:63`, `auralite-ffi/src/lib.rs:869`, `auralite-serialize/src/lib.rs:1639/1679`
- Sandbox: `interactive.rs` (record `:794/:768`, replay `:822`, browser `:878`, determinism `:981`), `replay.rs`, `visualizer.rs` (watermarked viewer), `Cargo.toml` (eframe optional, default-features off)
- Policy: `deny.toml`, `docs/dependencies.md`, `THIRD_PARTY_NOTICES.md`, `docs/adr/16,17-decision.md`, `.github/workflows/ci.yml` (pinned cargo-deny, fail-fast: false), `scripts/ci-local.sh`, `CONTRIBUTING.md`
