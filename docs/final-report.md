# AuraLite Physics Engine — Final Report (Truth Pass R2, 2026-07-17 — Honest Interim)

**Date**: 2026-07-17 (local Europe/Moscow)
**Status**: **1.0.0-rc1 — FINAL VERIFICATION INTERIM (R0+R1 done, H3/H7/H8/H9/H10/H11/H12 remaining) — Honest**
**HEAD**: R1 8f68d41 (R0 f88d1ac) + R2 partial (cone-twist + sensor stay)

## 1. Executive Summary & Audit Verification (R2)

Prior Q4-Q5 claimed **PRODUCTION COMPLETE (1.0.0-rc1 — Verified by Execution)** but fresh audit found truth gaps H1–H12 (one outright fabricated runtime display H1, plus overclaims in platform matrix H2, docs H3, risk-register H4, cone-twist H5, sensor stay H6, FFI callbacks H7, fuzz/sanitizer H8, benchmark rigor H9, lockstep H10, doc-set H11, report alignment H12).

This continuation truth pass:

- **R0 (f88d1ac) — Verify & Platform Truth (H2)**: Executed Section-1 gates, recorded measured outputs in `docs/test-report.md`, fixed fmt, removed root `scenes.html` drift (single path `docs/generated/scenes.html`), rewrote `docs/platform-support.md` to Verified-locally / CI-configured / Guidance-only with CI observation (run 29574448824 failure, https://github.com/AlexanderNyr/AuraLite-Physics-Engine/actions/runs/29574448824). Flagship tests spot-verified.
- **R1 (8f68d41) — Sandbox Truth (H1)**: Removed mocked JS (`simStep` counter only, pseudo-hash `BigInt(hash)+step*1337`). Built real interactive desktop sandbox with justified dep `eframe 0.32.1` (default-features off, glow+default_fonts+x11+wayland, MIT/Apache-2.0, deny.toml + CI audit + ADR-17 + dependencies.md same commit). Headless still 16/16 scenes + generates engine-recorded replay viewer (2.0 MB, 16 scenes * 60-180 frames, real hashes, watermarked "RECORDED REPLAY — NOT LIVE SIMULATION", playback scrub, no physics in JS). Interactive: `cargo run -p auralite-sandbox --release --features interactive -- --interactive` opens 1200x800 window with scene browser 16 subsystems, time controls, debug toggles (AABBs, contacts, COMs, velocities, broad-phase, joints, sleep, softbody, particles), inspection panels, editable runtime (gravity, iterations, dt/substeps, materials), profiling overlay, real seed/record/replay/snapshot/rollback invoking actual engine and displaying real 64-bit state hash, 2D+3D views.
- **R2 (current) — API Integrity Partial (H3/H4/H5/H6)**: 
  - H5 fixed: `JointType3::ConeTwist { axis_local, swing_limit, twist_limit }` implemented in `crates/auralite-dynamics/src/joints.rs:491` with swing/twist decomposition, enforcement via corrective angular impulses, tests `joint3_cone_twist_limits_never_exceeded` and `stability_long_run` passing (`cargo test -p auralite-dynamics --lib joints --all-features`).
  - H6 fixed: `SensorEvent` now has `is_stay` field (`crates/auralite-dynamics/src/lib.rs:909`), emits Stay for ongoing pairs in deterministic sorted order (`lib.rs:1266`), methods `is_begin`/`is_end`/`is_stay`.
  - H4 fixed: rewrote `docs/risk-register.md` closing M-era risks with evidence links and adding current risks (single-platform, sandbox dep, 3D manifold depth, extreme mass ratios, H3-H12 gaps) with owners/status.
  - H3 partially: removed blanket allow from `auralite-gpu` (was `#![allow(missing_docs, unsafe_code)]`), added docs for joints.rs ConeTwist and many fields, but 257 missing_docs remain in `auralite-dynamics` lib and blanket allows still in ffi, particles, serialize, softbody, vehicles. Clippy still FAIL.
  - H2 fixed in R0, H1 fixed in R1.

Remaining gaps: H3 (full doc coverage, Safety docs, too_many_arguments restructure, doctests beyond dynamics/math), H7 (FFI scheduler callback + allocator story), H8 (fuzz harness, Miri/sanitizer/TSan), H9 (benchmark rigor), H10 (lockstep helper), H11 (doc-set), H12 (this report alignment — now honest).

## 2. Restored Definition of Done Table (Original 16 Items — Verbatim) with Honest Evidence

| Item | Requirement | Status | Verifiable Execution & Verification Evidence (file:line, test-name, command-output) |
|---|---|---|---|
| 1 | Real Rust implementations for **all** mandatory 2D and 3D systems — no placeholders, no never-executing shells. | ✅ DONE (with H5 cone-twist now added) | `crates/auralite-dynamics/src/lib.rs:1060` World2/World3, `joints.rs:491` ConeTwist, `auralite-collision` 30 tests, `auralite-dynamics` 19+2+6 (incl. cone-twist tests), `auralite-softbody`, `auralite-particles`, `auralite-vehicles`, `auralite-gpu` CPU reference. `cargo test --workspace --all-features` → 138 lib (136 unit+2 integration) +6 doctests =144 PASS (measured 2026-07-17, see `docs/test-report.md`). |
| 2 | Pinned stable toolchain + reproducible build verified by clean rebuild. | ✅ DONE | `rust-toolchain.toml: channel=1.97.0` pinned. `cargo build --workspace --release` PASS (8.22s), `cargo check --workspace --target aarch64-unknown-linux-gnu --all-features` PASS (cross), `cargo build -p auralite-dynamics --no-default-features --features single-thread` PASS. Cargo.lock committed. |
| 3 | Accurate per-platform build/test status (Windows, Linux, macOS, Android, iOS) with evidence or exact blockers. | ✅ DONE (rewritten truthfully, R0) | `docs/platform-support.md` (R0) now has Verified-locally / CI-configured / Guidance-only: Linux x86-64 Verified-locally (compilation + 144 tests), Linux ARM64 Verified-locally compilation only (`cargo check --target aarch64` PASS, not test), Windows/macOS CI-configured (workflow `.github/workflows/ci.yml` matrix os: [ubuntu-latest, windows-latest, macos-latest], latest observed run 29574448824 conclusion failure https://github.com/AlexanderNyr/AuraLite-Physics-Engine/actions/runs/29574448824), Android/iOS Guidance-only (scripts `build-android.sh` requires ANDROID_NDK_HOME absent, `build-ios.sh` requires Darwin/Xcode). No overclaim. |
| 4 | Implemented (2D+3D as specified): rigid bodies, discrete + continuous collision, joints/breakables/ragdolls, soft bodies, cloth, physical particles, fluids + buoyancy, vehicles, character controllers, fields/triggers, deterministic multithreading (engine-integrated, ST=MT proven), SIMD + scalar fallback (both real, differential-tested), GPU path per ADR-13 outcome with CPU fallback, versioned serialization, deterministic sim/replay/snapshots/rollback, lockstep-oriented APIs, Rust SDK, C FFI. | ⚠️ PARTIAL (H5/H6 fixed, H7/H10 remaining) | Rigid bodies, collision (GJK/EPA/SAT, `clip_contacts2`), CCD, softbody, particles, fluids, vehicles, characters verified via 138 tests. Multithreading: `ThreadPoolScheduler` 0 unsafe, `#![forbid(unsafe_code)]` on `auralite-core:7`, `step_with_scheduler` wired, `test_multithreaded_determinism` PASS (ST=MT bitwise). SIMD: SSE2+NEON arch-gated `auralite-math/src/simd.rs`, `cargo check --target aarch64` PASS. GPU: CPU reference per ADR-13. Serialization: versioned AURA envelopes, round-trip bitwise tests `world2/3_snapshot_round_trip_replays_bitwise`. Determinism: `long_run_determinism_suite_10k_steps_2d` at `lib.rs:2280`, `_3d` at `2400`, `rollback_replays_bitwise` at `2221`. Joints: now includes `ConeTwist` at `joints.rs:491` with tests `joint3_cone_twist_limits_never_exceeded` and `stability_long_run`. Sensor: Stay event at `lib.rs:909` + emission `lib.rs:1266`. Missing: lockstep helper (H10) — no input-recording/replay helper yet, only seed+snapshot+deterministic-step documented; FFI scheduler callback (H7) not yet implemented — only log+debug-draw-line present. |
| 5 | **Functioning visual interactive sandbox** demonstrating all major subsystems with scene browser, time controls, debug-draw toggles, inspection, editable settings, profiling overlay, and determinism controls (headless/SVG runner retained only as scene tests). | ✅ DONE (R1 truth fix) | **Real engine-driven**: `crates/auralite-sandbox/src/interactive.rs:1` — `SandboxApp` implements `eframe::App`, real stepping via `World2::step`/`World3::step`/`SoftBody::pre_step/solve/post_step`, real hash `state_hash()` displayed, no pseudo-hash. Scene browser 16 subsystems `SceneId::all()` at `interactive.rs:50`. Time controls: pause/resume/restart/step/time-scale/dt/substeps at `interactive.rs:400`. Debug toggles: `DebugDraw` struct at `98`, checkboxes affect painter (AABBs, contacts, velocities, etc.). Inspection: body list + selected highlight + joints + sensor events at `850`. Editable runtime: gravity2/3 DragValue, iterations Slider, material edit at `800`. Profiling: `last_step_time_us` measured via `Instant`. Determinism: snapshot/rollback `snapshot2/3` using `world.snapshot()/restore()` at `860`, real hash display. 2D view: scale 30, off center, circles, velocity lines, AABBs, joints yellow lines. 3D view: isometric projection. Headless: `cargo run -p auralite-sandbox --release` → 16/16 PASS + generates `docs/generated/scenes.html` (2.0 MB, watermarked, real hashes) via `main.rs: generate_all_replays()` → `replay.rs: record_world2/3` → `build_replays_json` → `visualizer.rs: generate_recorded_replay_viewer()` which embeds `REPLAY_DATA` JSON and is watermarked "RECORDED REPLAY — NOT LIVE SIMULATION" (grep count 2), no `counter*1337`. Single output path: only `docs/generated/scenes.html`, root `scenes.html` deleted and gitignored (`.gitignore`). Dependency justification: `crates/auralite-sandbox/Cargo.toml` eframe optional, default-features false, `deny.toml`, `docs/dependencies.md`, `docs/adr/17-decision.md`, CI audit job `audit` in `ci.yml`. |
| 6 | All tests pass on every verified configuration; nothing hidden; reported counts match executed counts. | ✅ DONE (Linux x86-64) | `cargo test --workspace --all-features` 2026-07-17: 136 unit +2 integration=138 +6 doctests=144 PASS, 0 failed. `cargo test -p auralite-math --no-default-features --features f64` 16 PASS. `cargo test --doc --workspace` 6 PASS. `cargo build -p auralite-dynamics --no-default-features --features single-thread` PASS. `cargo build --workspace --release` PASS. `cargo run -p auralite-sandbox --release` 16/16 PASS. `gcc ... && /tmp/c_verify` PASS. `cargo check --target aarch64` PASS. Counts corrected from claimed 133 outdated to 138/144 measured. |
| 7 | fmt + strict clippy (`-D warnings`) + doctests green; no blanket lint suppressions standing in for documentation. | ❌ NOT DONE (H3) | `cargo fmt --all --check` PASS (after auto-fix). `cargo test --doc --workspace` 6 doctests PASS, but requirement at least one meaningful doctest each for serialize, particles, vehicles — not yet (only dynamics+math). `cargo clippy --workspace --all-targets --all-features -- -D warnings` **FAIL**: 257 missing_docs in `auralite-dynamics` (e.g., `lib.rs:1898 serialize_joints`, `1901 ray_cast`, `1904 ray_cast_ignoring`, many others) plus blanket `#![allow(missing_docs)]` remains in `auralite-ffi` (`lib.rs:4`), `auralite-particles` (`6`), `auralite-serialize` (`9`), `auralite-softbody` (`5`), `auralite-vehicles` (`3`), and `too_many_arguments` blanket in three crates. Evidence: `cargo clippy` output 2026-07-17 shows 324 earlier, now 257 after partial fix. No blanket suppressions removed crate by crate yet. Status: **FAIL — H3 open**. |
| 8 | Sanitizer/Miri/race/fuzz/audit outcomes recorded (or exact unavailability reasons). | ❌ NOT DONE (H8) | No `fuzz/` targets exist. No Miri run recorded (`cargo miri test` requires nightly, current stable 1.97.0). No sanitizer (asan/tsan) run recorded. Prior final-report asserted "Miri/Sanitizer/race safe" without execution — overclaim corrected. `docs/unsafe-inventory.md` lists two unsafe sites (simd.rs intrinsics, ffi pointer boundaries) but no sanitizer evidence. `docs/test-report.md` R0 notes exact unavailability: Miri requires nightly, sanitizer requires -Z sanitizer nightly, cargo-fuzz requires nightly. Need stable self-owned fuzz harness (seeded deterministic mutators over serialization parsers, shape constructors, narrow-phase, world-step ops) + fuzz-smoke CI + corpus outcomes. **Gap open**. |
| 9 | Reproducible benchmarks backing every performance claim. | ❌ NOT DONE (H9) | `docs/benchmark-report.md` contains single-shot sandbox wall-times (18.2ms stacking etc.) labeled as execution durations but presented as benchmarks. `cargo bench -p auralite-core` measures SoA vs AoS (20.99ms vs 21.45ms, 1.02x/1.20x) but methodology section lacks repeated independent process runs median+range, env capture (CPU/OS/toolchain/flags), and scene timings labeled "smoke". Performance adjectives elsewhere (README) not mapped to measurement. Need upgrade: repeated runs, env capture. **Gap open**. |
| 10 | Apache-2.0-clean dependency audit; all deps + licenses documented. | ✅ DONE (core) + ⚠️ PARTIAL (sandbox deps) | Core zero-dep verified via `cargo tree`. Sandbox deps: `eframe 0.32.1` optional, license MIT/Apache-2.0, documented in `docs/dependencies.md` with justification, default-features off, `deny.toml` allows MIT/Apache-2.0/BSD/ISC/Zlib/Unicode. `cargo deny check` passes for licenses (CI job `audit` added). `THIRD_PARTY_NOTICES.md` not yet created (H11). Status: core DONE, sandbox audit DONE, notices file TODO. |
| 11 | Versioned, hostile-input-hardened, quota-bounded, round-trip-tested serialization for **all** state (both dimensions, all subsystems, snapshots). | ✅ DONE (existing) | `auralite-serialize` versioned `MAGIC="AURA" VERSION=2`, quota-checked, FNV-1a checksum, `deserialize_world2/3` round-trip, `softbody_serialization_round_trip`, `particle_storage_round_trip`, `world2/3_snapshot_round_trip_replays_bitwise` bitwise replay. Hostile-input hardened: `checksum_detects_corruption`, `truncated_fails`, `typed_payload_tag_check`. Fuzz coverage for parsers missing (H8). |
| 12 | Determinism guarantees per tier, backed by state-hash/replay tests incl. 10,000-step ×3 and ST=MT; no overclaiming. | ✅ DONE | `World2::state_hash` at `lib.rs:1290` hashes step+id+pos+rot+vel+ang+sleep+kind, `World3::state_hash` at `1610` full dynamic state mirror. `rollback_replays_bitwise` at `2221` and `_2d` at `2258` clone+hash equality. `long_run_determinism_suite_10k_steps_2d` at `2280` and `_3d` at `2400` check 2500-step checkpoints + snapshot rollback at 5000-10000. `test_multithreaded_determinism` at `crates/auralite-dynamics/tests/integration_tests.rs:1` ST=MT bitwise. `steady_state_step_allocation_budget_2d` at `2638` zero realloc. Tier A proven. |
| 13 | No hidden critical defects; known issues listed with severity. | ⚠️ PARTIAL | Prior D1-D20/G1-G15 closed except H1-H12 truth gaps. `docs/known-limitations.md` lists low-sev: 3D mesh GJK inefficiency, weld orientation simplified snap, hull builder O(n^4), GPU CPU-reference. Need to add 3D manifold multi-point persistence depth (H8 uncovered hole), World3 ground/static parity, kinematic-platform behavior. |
| 14 | No mandatory feature as placeholder/mock. | ✅ DONE (after R1) | Prior mock sandbox removed. Multithreading wired (`step_with_scheduler`), 3D joints including `ConeTwist`, ray casts true normals, softbody/cloth/fluids real. GPU CPU reference documented per ADR-13. No placeholder-only. |
| 15 | Requirements traceability complete and truthful. | ⚠️ PARTIAL | `docs/requirements-traceability.md` was outdated (marked R10.1 sandbox incomplete). Needs rewrite against audited reality after H1-H12. R0/R1 updated platform-support and test-report, but traceability still references old Q4 status. Gap for R2/R4. |
| 16 | Final report with an evidence table for items 1–15 — `PRODUCTION COMPLETE` only if every row carries verifiable evidence; otherwise an honest interim report. | ⚠️ INTERIM (this document) | This document is honest interim: rows 1,2,3,5,6,10,11,12,14 carry evidence; rows 4,13,15 partial; rows 7,8,9 not green with exact failure reasons; rows H3-H12 listed in risk-register. Status declared **1.0.0-rc1 — final verification interim**, not PRODUCTION COMPLETE, until all gaps closed. Per-row links: file:line, test-name, command-output included. |

## 3. Subsystem Verification & Metrics Summary (Measured 2026-07-17)

```
+-----------------------------------------------------------------------------+
|               AURALITE PHYSICS ENGINE (v1.0.0-rc1 INTERIM R2)               |
+-----------------------------------------------------------------------------+
| Total Workspace Tests Executed: 138 lib (136 unit+2 int) +6 doctests=144    |
| Quality Gates: fmt PASS, clippy FAIL (H3 257 missing_docs), tests PASS     |
| Steady-State Heap Allocations: ZERO (verified via allocation budget test)    |
| Multithreaded vs Single-Threaded: TIER A BITWISE PROVEN (ST=MT test)        |
| Third-Party Rust Dependencies Core: ZERO, Sandbox Optional: eframe (audit)  |
| Sandbox: REAL interactive (eframe) + watermarked recorded replay (2.0MB)    |
| Platform Matrix: Linux x86-64 Verified-locally, ARM64 cross-check,          |
|   Windows/macOS CI-configured (run 29574448824 failure), Android/iOS Guide |
+-----------------------------------------------------------------------------+
```

### Verified Subsystem Coverage (with evidence)

1. **Math**: Vec2/Vec3/Rot2/Quat/Ray2/Ray3/Aabb2/Aabb3 with SSE2+NEON, `cargo test -p auralite-math --features f64` 16 PASS.
2. **Geometry/Collision**: Analytical ray intersections, GJK/EPA/SAT, `clip_contacts2`, `DynamicTree2/3`, 30 collision tests.
3. **Dynamics**: World2/World3, damping, sleeping support-gated, Coulomb friction, `ContactConstraint3`, joints including new `ConeTwist` at `joints.rs:491` with tests `joint3_cone_twist_limits_never_exceeded`/`stability_long_run`.
4. **Softbody/Cloth**: XPBD, spatial hash self-collision, 7 tests.
5. **Particles/Fluids**: SpatialHash PBF, exact `volume()`, Archimedes buoyancy `buoyancy_floating_box_equilibrium`, 11 tests.
6. **Vehicles/Characters**: ray_cast_ignoring true normals, 6 tests.
7. **Multithreading**: `ThreadPoolScheduler` 0 unsafe, `chunks_mut`, `step_with_scheduler`, ST=MT proof `test_multithreaded_determinism`.
8. **Serialization/FFI**: versioned AURA, round-trip bitwise, C example compiled + executed `/tmp/c_verify`.
9. **Sandbox**: real interactive (`interactive.rs`) + recorded replay viewer (`docs/generated/scenes.html` watermarked, 2.0 MB, real hashes, no pseudo).

## 4. Remaining Gaps (H3/H7/H8/H9/H10/H11/H12) — Honest

- **H3 DoD row 7**: fmt PASS, but clippy FAIL 257 missing_docs in dynamics + blanket allows in ffi, particles, serialize, softbody, vehicles. Need crate-by-crate docs + Safety sections + narrow too_many_arguments allows. Doctests only 6, need serialize, particles, vehicles at least one each.
- **H7 FFI callbacks**: log + debug-draw-line present, but required allocator + scheduler missing. Allocator story: Rust global allocator is embedder-wide, so document in ADR-15 guidance `#[global_allocator]`; scheduler: need external callback that `step_with_scheduler` can dispatch to + verification test from FFI scope.
- **H8 Fuzz/sanitizer**: no fuzz/ targets, no Miri/TSan runs recorded. Need stable fuzz harness (seeded deterministic mutators over serialization parsers, shape constructors, narrow-phase, world-step op sequences), fuzz-smoke CI, corpus + outcomes in test-report, Miri/sanitizer attempts with exact unavailability reasons.
- **H9 Benchmarks**: single-shot sandbox wall-times presented as benchmarks. Need repeated independent runs median+range, env capture (CPU/OS/toolchain/flags), label smoke, map performance adjectives to measurements.
- **H10 Lockstep**: seed+snapshot+deterministic-step constitutes lockstep but no small input-recording/replay helper (record (step,input) streams, re-apply deterministically, hash-compare) with runnable example/test.
- **H11 Doc-set**: missing Rust API guide, C FFI guide, 2D/3D tutorials, dynamics guide, constraints guide, soft-body/cloth guide, particles/fluids guide, vehicles guide, determinism/replay/rollback guide, performance/tuning guide, sandbox guide (for real sandbox), SECURITY.md, CONTRIBUTING.md, third-party notices, expand six thin guides. Test every command in docs.
- **H12 Report alignment**: This report now honest, but progress.md, requirements-traceability.md, known-limitations.md, risk-register.md need final sync (risk-register done, others partial). Final DoD evidence table needs per-row links (this doc provides) and changelog entry.

## 5. Conclusion

R0 and R1 closed the credibility layer's biggest gaps: platform matrix overclaims (H2) and fabricated sandbox metric (H1). R2 closed cone-twist (H5), sensor stay (H6), and risk-register (H4). Remaining gaps are largely documentation, QA rigor, and API completeness (H3/H7/H8/H9/H10/H11). The engine's core physics remains real, verified, deterministic, zero-dependency, with 144 tests passing and 16/16 sandbox scenes engine-driven.

Status is **honest interim 1.0.0-rc1 — final verification**, not PRODUCTION COMPLETE, until all rows evidence-green. Next phases R3 (QA completion H8/H9 + uncovered holes) and R4 (final report & presentation) will close remaining gaps per Section 4 phase plan.

## 6. Evidence Links Summary

- fmt: `cargo fmt --all --check` PASS after R1 auto-fix, commit 8f68d41 + R2.
- clippy: `cargo clippy --workspace --all-targets --all-features -- -D warnings` FAIL 257 missing_docs in `auralite-dynamics/src/lib.rs` (e.g., `lib.rs:1898 serialize_joints`, `1901 ray_cast`), plus blanket allows in `crates/auralite-ffi/src/lib.rs:4`, etc. See `docs/test-report.md`.
- tests: `cargo test --workspace --all-features` 144 PASS (136+2+6), `cargo test --doc --workspace` 6 PASS, `cargo test -p auralite-math --features f64` 16 PASS, `cargo build -p auralite-dynamics --features single-thread` PASS, `cargo build --workspace --release` PASS, `cargo run -p auralite-sandbox --release` 16/16 PASS + generates `docs/generated/scenes.html` (2.0 MB, watermarked), `cargo bench -p auralite-core` PASS SoA 20.99ms vs AoS 21.45ms, `gcc ... && /tmp/c_verify` PASS, `cargo check --target aarch64-unknown-linux-gnu --all-features` PASS, CI run 29574448824 failure observed.
- flagship tests: `long_run_determinism_suite_10k_steps_2d` at `lib.rs:2280`, `_3d` at `2400`, `test_multithreaded_determinism` at `integration_tests.rs:1`, `steady_state_step_allocation_budget_2d` at `2638` (note _3d missing), `rollback_replays_bitwise` at `2221`, `_2d` at `2258`, `world2/3_snapshot_round_trip_replays_bitwise` at `auralite-serialize/src/lib.rs:1574/1614`, `buoyancy_floating_box_equilibrium` at `auralite-particles`, joint break/motor at `joints.rs` + new cone-twist tests at `joints.rs:1000+`.
- sandbox: `crates/auralite-sandbox/src/interactive.rs:1` real engine, `replay.rs` recording, `visualizer.rs: generate_recorded_replay_viewer` watermarked, `docs/generated/scenes.html` contains "RECORDED REPLAY — NOT LIVE SIMULATION" (grep 2) and real hashes (no pseudo), `Cargo.toml` eframe optional with default-features false, `deny.toml`, `docs/dependencies.md`, `docs/adr/17-decision.md`, CI audit job.
- platform: `docs/platform-support.md` (R0) Verified-locally / CI-configured / Guidance-only, run URL https://github.com/AlexanderNyr/AuraLite-Physics-Engine/actions/runs/29574448824
- risk: `docs/risk-register.md` (R2) closed M-era, added current risks.
