# Dependencies
There are no third-party Rust dependencies. All crates use only `core`/`std`; therefore no direct third-party licenses are present. Rust toolchain components are build tools and are not redistributed. A future graphical sandbox/GPU implementation must add a decision record and license audit before dependencies are accepted.
