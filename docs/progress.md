# Progress
## Current milestone: M3 in progress; M0–M2 complete locally
M0 and M1 remain green. **M2 is complete as of 2026-07-16.** `auralite-geometry` implements validated native 2D circles, edges/chains, capsules, boxes, convex polygons/hull construction, compounds, heightfields and half-spaces; native 3D spheres, edges, capsules, boxes, convex-polyhedron hull construction, compounds, triangle meshes with deterministic BVH, grid heightfields and half-spaces. Convex support, local/world bounds, bounding radii, containment, closest point, ray tests, scaling rules, analytical primitive mass, parallel-axis compound mass and numerical closed mesh/hull mass are implemented. Static/infinite/zero-dimensional shapes explicitly have no finite mass/bounds where mathematically undefined.

M2 acceptance passed: all constructors reject malformed/non-finite/degenerate inputs; analytical mass checks cover circle/sphere/box and tetra/cube volume; 10,000-direction randomized support tests compare against sampled/vertex references; transformed bounds contain corners; closest/ray/scaling rules and malformed assets are tested. A cube-hull overcount and mesh ray parity double-hit found during development became regression tests. Full gates passed: `cargo fmt --all --check`; `cargo test --workspace --all-features` (45 tests, 0 failed); isolated f64 math (11 tests); strict all-feature clippy; release build. M2 benchmark median: 14.949 ns Box3 support and 17.714 ms deterministic BVH build for 7,938 triangles.

M3 work in this session: deterministic height-balanced dynamic AABB trees in native 2D/3D with velocity-predicted fat bounds, add/update/remove/query/ray/pairs, canonical IDs, and insertion-order invariance; symmetric layers/masks/groups/sensor filtering with canonical callback order; exact bounded circle/sphere and plane TOI diagnostics. The tree differential test performs 100,128 pair checks. Bounded native 2D/3D GJK distance now returns witnesses, residuals, iteration status, finite degenerate fallbacks, and matches 1,000 analytical circle separations. A first-iteration infinity convergence defect found by the overlap test was fixed and retained as regression coverage. M3 is **not complete**: 2D SAT and bounded EPA now pass 1,000 depth comparisons; persistent feature-ID manifolds and robust 3D segment pairs are implemented. Remaining: full 3D SAT/penetration fallback, clipping/dispatch, accelerated mesh mid-phase, complete scene/batched queries and robustness battery.

## Resume pointer
Continue M3 from `auralite-collision`: dynamic trees/filtering/analytic sphere CCD are green. GJK plus 2D SAT/EPA and manifold persistence are green. Next implement 3D SAT/penetration fallback, face clipping and complete pair dispatch. After that wire BVH mid-phase, scene/batched overlap-distance-shape casts and conservative advancement. Complete the robustness battery and M3 benchmarks. Preserve reference paths. Do not mark M3 complete until every M3 acceptance item passes.
