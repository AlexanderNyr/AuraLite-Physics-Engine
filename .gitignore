/target
*.profraw
.DS_Store
# Root scenes.html is deprecated drift — canonical is docs/generated/scenes.html (H1 fix)
# Ignore stale root artifact if regenerated by old scripts
/scenes.html
# But keep docs/generated/scenes.html committed reproducibly as watermarked recorded-replay viewer
!docs/generated/scenes.html
